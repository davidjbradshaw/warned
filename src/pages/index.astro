---
// Language Selector Landing Page with Video Background
const base = import.meta.env.BASE_URL;

// This page replaces the old index.astro and provides language selection
// Video: The original site used "Warned-animation.mp4`} (23.3s, looping)
// TODO: Add the actual video file to /public/videos/warned-animation.mp4
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WARNED - We Are Retired Not Expired</title>
  <meta name="description" content="Empowering Belgian 'Goldies' (65+). We fight ageism through social hubs, intergenerational events, and skill-sharing. Because we are Retired, Not Expired!">

  <!-- Open Graph -->
  <meta property="og:title" content="WARNED - We Are Retired Not Expired">
  <meta property="og:description" content="Empowering Belgian 'Goldies' (65+). We fight ageism through social hubs, intergenerational events, and skill-sharing.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="/images/logo.png">

  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Dosis:wght@300;400;500;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="video-container">
    <!-- Video Background -->
    <video class="background-video" autoplay muted loop playsinline poster="/images/hero-background.jpg">
      <source src={`${base}videos/warned-animation.mp4`} type="video/mp4">
      <!-- Fallback: If video doesn't load, the poster image will show -->
    </video>

    <!-- Overlay Content -->
    <div class="content-overlay">
      <div class="language-buttons">
        <a href="/en/" class="language-button">English</a>
        <a href="/fr/" class="language-button">Français</a>
        <a href="/nl/" class="language-button">Nederlands</a>
      </div>
    </div>
  </div>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Dosis', sans-serif;
      overflow: hidden;
      height: 100vh;
    }

    .video-container {
      position: relative;
      width: 100%;
      height: 100vh;
      overflow: hidden;
      background-color: #000;
    }

    .background-video {
      position: absolute;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      transform: translate(-50%, -50%);
      z-index: 0;
      object-fit: cover;
    }

    /* Fallback background image if video doesn't load */
    .video-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      z-index: -1;
    }

    .content-overlay {
      position: relative;
      z-index: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: rgba(0, 0, 0, 0.3);
    }

    .language-buttons {
      display: flex;
      gap: 2rem;
      flex-wrap: wrap;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }

    .language-button {
      display: inline-block;
      padding: 1rem 3rem;
      font-size: 1.5rem;
      font-weight: 500;
      color: #fff;
      background: rgba(255, 255, 255, 0.1);
      border: 2px solid #fff;
      border-radius: 50px;
      text-decoration: none;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .language-button:hover {
      background: rgba(255, 255, 255, 0.9);
      color: #333;
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }

    .language-button:active {
      transform: translateY(-2px);
    }

    /* Mobile responsiveness */
    @media (max-width: 768px) {
      .language-buttons {
        flex-direction: column;
        gap: 1.5rem;
      }

      .language-button {
        font-size: 1.25rem;
        padding: 0.875rem 2.5rem;
        width: 100%;
        max-width: 300px;
        text-align: center;
      }
    }

    /* Ensure video is not visible on mobile to save bandwidth */
    @media (max-width: 768px) {
      .background-video {
        display: none;
      }
    }
  </style>
</body>
</html>
